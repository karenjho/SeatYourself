<% if current_user %>
  <%= form_for ([@restaurant, @review]) do |f| %>

    <div class="field">
      <%= f.label :comment %>
      <%= f.text_area :comment %>
    </div>

    <div class="actions">
      <%= f.submit "Add Review" %>
    </div>

  <% end %>
<% end %>
